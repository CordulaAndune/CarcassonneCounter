<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ScoreStyle" >
        <!-- Style for the Score TextViews -->
        <item name="android:textSize">@dimen/scoreTextSize</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="PopupTextStyle" >
        <!-- Style for the Score TextViews -->
        <item name="android:textSize">@dimen/popupTextSize</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:drawablePadding">@dimen/buttonPadding</item>
        <item name="android:layout_margin">@dimen/outer_padding</item>
    </style>

    <style name="TilesScore">
        <!-- Style for Tiles EditText -->
        <item name="android:background">@drawable/tiles_edittext</item>
        <item name="android:textSize">@dimen/scoreTextSize</item>
        <item name="android:inputType">number</item>
    </style>

    <style name="MainButtons">
        <!-- Style for Tiles Buttons on main screen -->
        <item name="android:textSize">@dimen/buttonTextSize</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/main_buttons_normal</item>
        <item name="android:layout_margin">@dimen/buttonPadding</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ChangeButtons">
        <!-- Style for Increment and decrement buttons -->
        <item name="android:textSize">@dimen/buttonTextSize</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="PopupButtons">
        <!-- Style for Tiles Buttons on main screen -->
        <item name="android:textSize">@dimen/buttonTextSize</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/main_buttons_normal</item>
        <item name="android:layout_margin">@dimen/buttonPadding</item>
    </style>

    <style name="ColorButtonLayout">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:layout_margin">24dp</item>
    </style>
</resources>
